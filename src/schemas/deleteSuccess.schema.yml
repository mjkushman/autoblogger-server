components:
  schemas:
    DeleteSuccess:
      type: object
      properties:
        status:
          type: number
          example: 200
        message:
          type: string
          example: "Success"
        data:
          type: object
          properties:
            message:
              type: string
              nullable: true
              enum: ["Delete Successful", null]